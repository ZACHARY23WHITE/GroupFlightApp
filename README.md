# GroupFlightApp

Family / group trip planner: shared trips, travelers (home airport, party size, cabin), and Google Flights–style quotes via SerpAPI.

See **`web/`** for the Next.js app (`npm install` and `npm run dev` inside `web/`). Copy `web/.env.example` to `web/.env` and add your keys.
