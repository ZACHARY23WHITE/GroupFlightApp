import { ProfileForm } from "./profile-form";

export const metadata = {
  title: "Profile · Group trip planner",
  description: "Edit your travel defaults and contact information.",
};

export default function ProfilePage() {
  return <ProfileForm />;
}
