rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through Next.js + Firebase Admin SDK. No direct client reads/writes.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
